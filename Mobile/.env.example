# Base URL for the ML service (ml/app.py). Only needed when the default
# per-platform guess in src/api/config.ts is wrong for your setup — e.g. a
# physical device, which needs your machine's LAN IP instead of localhost.
# EXPO_PUBLIC_API_URL=http://192.168.1.23:8000
