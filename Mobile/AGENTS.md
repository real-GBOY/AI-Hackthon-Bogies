# Expo SDK pinned to 56

This project was downgraded from SDK 57 to SDK 56 (react-native 0.85.3, react 19.2.3). Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code, and use `npx expo install <pkg>` so added packages resolve to SDK 56-compatible versions.
